int media(float notas[], int quantidade)
{
    quantidade = (quantidade == NULL) ? quantidade : 6;
    int i;
    int tamanho = sizeof(notas);
    printf("%d\n",tamanho);
    return tamanho;

    return retorno;
}

